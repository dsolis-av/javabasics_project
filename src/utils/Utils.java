package utils;

import java.util.Random;

public class Utils {
    //here we define a shared Random static instance to avoid creating multiple objects everytime we wanted to generate random numbers
    public static Random rand = new Random();
}
